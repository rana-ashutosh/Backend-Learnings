<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="h-full w-full p-10 bg-zinc-900">
        <div class="mb-10">
            <a class="text-blue-500" href="/">Back to home</a>
        </div>
        <h1 class="text-3xl tracking-tighter text-white mb-3">All Users</h1>
        <div class="users flex gap-3 flex-wrap">
            <% if(allusers.length > 0){ %>
                 <% allusers.forEach(function(user){ %>
                <div class="user w-72 p-4 bg-zinc-800 rounded-lg">
                    <div class="w-full h-72 rounded-lg bg-zinc-700 overflow-hidden">
                        <img class="h-full w-full object-cover" src="<%= user.image %>" alt="">
                    </div>
                    <h3 class="text-white mt-3 tracking-tighter text-3xl"><%= user.name %></h3>
                    <h5 class="text-white text-sm"><%= user.email %></h5>
                    <div class="flex justify-between mt-3">
                        <a class="text-white" href="/edit/<%= user._id %>">Edit user</a>
                        <a class="text-red-600" href="/delete/<%= user._id %>">Delete user</a>
                    </div>
                </div>
            <%  }) %>
          <% } else{ %>
            <h3 class="text-white text-lg">No user yet</h3>
         <% }%>
           
        </div>
    </div>
</body>
</html>